# CLAUDE.md

@AGENTS.md

The imported `AGENTS.md` holds all project rules — commands, layout, style,
testing, commit conventions, pinned versions and working rules. It is the single
source of truth for every agent; keep changes there rather than duplicating them
here, so the two files cannot drift apart.

Add Claude Code specifics below only when they genuinely do not belong in the
shared file.
